name: Crime Data CICD
on:
  push:
    branch:
      - main

  workflow_dispatch:

jobs:
  Testing:
    runs-on: ubuntu-latest
    steps:
      - name: Test Workflow
        uses: ./.github/workflows/test.yml

  Dev:
    runs-on: ubuntu-latest
    needs: Testing
    steps:
      - name: Dev
        run: echo "hi"